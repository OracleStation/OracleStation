# merger hooks, run tools/hooks/install.bat or install.sh to set up
*.dmm merge=dmm
*.dmi merge=dmi

# shell scripts need just lf
*.sh eol=lf

# force changelog merging to use union
html/changelog.html merge=union
